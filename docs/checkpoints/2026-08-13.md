---
title: Checkpoint — ARCANUM — 2026-08-13 — Nombre y Umbral
date: 2026-08-13
tags:
  - checkpoint
  - arcanum
  - nombre-y-umbral
status: bloqueado-gates
---

# Checkpoint — ARCANUM — 2026-08-13 — Nombre y Umbral

## Resumen

Fase A implementada en worktree limpio desde `origin/release/p0a-beta@84ad664`: perfil local cifrado, catálogo de 20 nombres, conversor español, gematría estándar y flujo dentro de Perfil. Código y pruebas quedan sin commit porque APK y prueba física no están verdes.

## Cambios realizados

- Perfil de lectura separado de `display_name`, con hasta tres nombres y tres apellidos.
- Persistencia local AES-256-GCM sobre almacenamiento seguro. Nombre original, hebreo, pronunciación, resultados e historial quedan dentro del payload cifrado.
- Conversor fonético `phon-he-1.0.0` para español Colombia/LatAm y peninsular. Ambigüedades `x`, `ll` y `h` preguntan antes de convertir.
- Portugués bloqueado con «próximamente». Vasco exige forma manual.
- Gematría `mispar-hechrechi-1.0.0`, valores 1–400 y finales regulares 20/40/50/80/90.
- Catálogo cerrado de 20 fichas verificadas con cita, certeza, atribución y enlace. Apellidos no reciben etimología.
- Flujo `Perfil > Identidad > Nombre y Umbral > Archivo / Letras / Confirmar / Cálculo` sin nueva pestaña.
- Exportación local al portapapeles y borrado de perfil. Borrado de cuenta también elimina perfil local.
- Tres capturas golden: Identidad, lista Nombre y Umbral, Archivo/Cálculo.
- Pruebas de gematría, historial inmutable, cifrado, catálogo, ambigüedad, separación de `display_name`, aislamiento HTTP y ausencia de dependencias desde Tarot/Cielos.

## Estado actual

- `flutter analyze lib`: verde, cero issues.
- `flutter test`: verde, 213 pruebas.
- Pruebas del módulo: verde, 19 pruebas.
- `git diff --check`: verde.
- CJK: limpio.
- `flutter build apk --debug`: bloqueado. JDK configurado inicialmente falla en `jimage.dll`; JBR 21 supera Java/Gradle, pero el worktree limpio no contiene `android/app/google-services.json`.
- OnePlus detectado y autorizado: `GN2200`, Android 12. La prueba física sigue pendiente porque no existe un APK nuevo instalable.
- Sin commit ni push: los gates obligatorios no están completos.

## Pendientes / próximos pasos

- Proveer `google-services.json` local de desarrollo fuera de Git o ejecutar el gate en entorno que ya lo tenga. No copiar ni commitear secretos/configuración privada.
- Repetir APK.
- Instalar APK en OnePlus y probar crear, convertir, editar, confirmar, cerrar/abrir, exportar y borrar.
- Revisar `logcat` con nombre canario: cero texto original, hebreo o valor.
- Solo entonces: stage selectivo, commit y push de `feat/nombre-y-umbral-foundation`.

## Decisiones y notas

- Ningún dato de Nombre y Umbral usa cliente HTTP, analytics o Crashlytics.
- Tarot y Cielos no importan el módulo nuevo.
- Cada cambio de grafía crea resultado nuevo y conserva la versión previa.
- La forma contemplativa nunca se etiqueta histórica.
- El catálogo no contiene frecuencia INE porque no se extrajo un conjunto verificable; tampoco la mezcla con significado.

## Git

- Worktree: `D:\tmp\nombre-y-umbral-foundation`.
- Rama: `feat/nombre-y-umbral-foundation`.
- Base: `84ad66476870aa46a3e4d32399b18a22965a5b71`.
- Estado: cambios sin commit por gates incompletos.

---
Tags: #checkpoint #arcanum #nombre-y-umbral
