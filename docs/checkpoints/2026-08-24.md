---
title: "Checkpoint — ARCANUM Hoy — 2026-08-24"
date: 2026-08-24
tags: [checkpoint, arcanum, hoy, flutter]
---

# Checkpoint — ARCANUM Hoy — 2026-08-24

Relacionado: [[MOC-ARCANUM]]

## Resumen

El rediseño seguro de Hoy quedó implementado en `fix/bogota-release`: cascada cancelable, preview tocable, instrumento celeste anidado, sello con consentimiento y ruedas reales de nivel 3. No hubo cambios de backend ni despliegues.

## Cambios realizados

- `_CascadeState` conserva y cancela su `Timer` al desmontarse.
- Hoy reúne regente, hora planetaria y Luna en un instrumento estático de relojes concéntricos.
- Sin coordenadas no se consulta `/today`, no se inventan ciudad, regente ni hora; la fase lunar real permanece visible.
- El sello usa `today` y `chapter`, exige consentimiento antes de `horoscope()` y solo solicita `celestialOverview()` después de abrir correctamente.
- Nivel 3 dibuja una rueda fija en Aries con longitudes reales y el glifo del aspecto en el centro.
- **Los modernos siguen sin filtrar, y eso contradice la decision de usar solo cuerpos clasicos.** El nivel 3 pinta lo que llegue del servidor, y el servidor manda Urano, Neptuno y Pluton; `transit_weight.py:67` le da a Urano un peso de 0,95, asi que puede salir de capitulo. No es un fallo de este trabajo -- el filtro del motor era un pendiente aparte y quedo fuera de encargo a proposito -- pero hasta que se haga, la pantalla puede ensenar un planeta moderno.
- Preview local: `D:\Proyectos\Arcanum-release\.tmp\diseno\hoy_codex_preview.html`.

## Estado actual

- Rama: `fix/bogota-release`.
- Commits funcionales: `2ebb2a9`, `1aa1631`, `52ae4d1`, `869809d`.
- `flutter analyze --no-pub`: limpio.
- `flutter test test/features/hoy/ --no-pub`: 50/50.
- `flutter test test/core/theme/glifos_fallback_test.dart --no-pub`: 5/5.
- `flutter test --no-pub`: 331/331.
- Cero fallos, excepciones o timers pendientes.

## Pendientes / próximos pasos

- Implementar como tarea backend separada la persistencia auditable del consentimiento: columnas en `users`, migración Alembic y sincronización.
- Integrar la rama al flujo de release solo cuando corresponda; este trabajo no desplegó nada.

## Decisiones y notas

- Groq es el proveedor del oráculo; `claude_service.py` conserva un nombre heredado.
- El consentimiento rechazado deja el sello cerrado y produce cero llamadas a `horoscope()` y `celestialOverview()`.
- Los cambios generados ajenos que ya estaban sucios en el worktree quedaron fuera de todos los commits.

## Git

Cuatro commits funcionales aislados fueron verificados y subidos a `origin/fix/bogota-release`. Este checkpoint se añade en un commit documental separado.

## Ubicación

- Proyecto: `D:\Proyectos\Arcanum-release\docs\checkpoints\2026-08-24.md`.
- Vault: `D:\Brain\10-Proyectos\ARCANUM\Checkpoint-2026-08-24-2.md`.

---
Tags: #checkpoint #arcanum #hoy
