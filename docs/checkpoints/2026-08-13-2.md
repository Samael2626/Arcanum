---
title: "Checkpoint — ARCANUM — 2026-08-13 — Nombre y Umbral verificado"
date: 2026-08-13
tags: [checkpoint, arcanum, nombre-y-umbral]
status: listo-para-commit
---

# Checkpoint — ARCANUM — 2026-08-13 — Nombre y Umbral verificado

## Resumen

Nombre y Umbral quedó probado en OnePlus GN2200. Se corrigió el fallo real de Flutter que podía dejar la aplicación en pantalla roja al crear el árbol de widgets. Samuel ahora se presenta como archivo vivo: significado, relato y letras; sin URL cruda ni lenguaje que vacíe la experiencia.

## Cambios realizados

- `main.dart`: `ensureInitialized` y `runApp` ahora viven en la misma zona. Se eliminó el `Zone mismatch` que causaba la aserción `_dependents.isEmpty`.
- En debug, los errores Flutter se muestran en consola; en release siguen reportándose a Crashlytics.
- `Nombre y Umbral`: «Cada nombre guarda una historia» reemplaza «sin destino prefabricado».
- Ficha Samuel: «Dios ha escuchado», relato de Ana, lectura tradicional `שמע` / *shama* / escuchar y `אל` / *El* / Dios.
- Enlaces y URL visibles retirados. Cita, certeza y límite viven dentro de «Nota de archivo».
- Prueba nueva protege relato Samuel y ausencia de URL cruda. Goldens actualizados.

## Estado actual

- OnePlus GN2200: abrir Samuel, volver, reinicio frío de Nombre y Umbral y Nota de archivo: verdes.
- `logcat`: cero `Zone mismatch`, `_dependents.isEmpty`, `Failed assertion` y `FATAL EXCEPTION` en la prueba final.
- `flutter analyze lib`: verde, cero issues.
- `flutter test`: verde, 215 pruebas.
- `flutter build apk --debug`: verde.
- `git diff --check`: verde.
- `google-services.json`: local, ignorado y no rastreado.

## Pendientes

- Configurar RevenueCat real antes de beta externa: el debug continúa mostrando `Invalid API Key` por el placeholder conocido.
- No integrar Nombre y Umbral con Tarot, Cielos u Oráculo hasta definir consentimiento puntual y contrato simbólico.
- Catálogo permanece en 20 nombres verificados; apellidos siguen sin etimología publicada.

## Decisiones y notas

- La magia vive en relato, forma y exploración; no en afirmar destino automático.
- «Lectura tradicional de las letras» queda separada de la etimología filológica.
- Fuentes continúan disponibles, pero no invaden la pantalla ni exponen URLs.

## Git

- Worktree: `D:\tmp\nombre-y-umbral-foundation`.
- Rama: `feat/nombre-y-umbral-foundation`.
- Base: `84ad66476870aa46a3e4d32399b18a22965a5b71`.
- Pendiente: stage selectivo, commit y push; artefactos generados y Firebase quedan fuera.

---
Tags: #checkpoint #arcanum #nombre-y-umbral
