# Checkpoint — Arcanum — 2026-06-16

## Resumen
Snapshot del estado actual del proyecto. Sin avance nuevo registrado desde el checkpoint anterior
(2026-06-15, ver `ARCANUM-Semana1-Progreso.md` en el vault). Este checkpoint sirve como punto de
referencia, no documenta trabajo de sesión.

## Estado actual
- **Backend (`arcanum-api/`)**: Semana 1 completa a nivel de código — estructura FastAPI, 8 modelos
  SQLAlchemy, auth JWT (access 15 min + refresh 30 días), endpoints de `auth` y `users`, migraciones
  Alembic (`env.py`, `script.py.mako`, `versions/001_initial.py`). No verificado en runtime (sin
  Python/PostgreSQL/Redis en este entorno) — validado solo por revisión estática completa.
- **App móvil (`arcanum-app/`)**: esqueleto mínimo (`app/models/`), Semana 3 (Flutter) aún no
  iniciada.

## Pendientes / próximos pasos
- Validar el backend en una máquina con Python/Docker: `uvicorn app.main:app --reload`, probar
  `/docs`, `/auth/register`, `/auth/login`, `/users/me`, `/auth/refresh`.
- Correr `alembic revision --autogenerate` y `alembic upgrade head` contra un Postgres real.
- Continuar con Semana 2: Motor astral (AstroVisor API + horas planetarias + fases lunares).

## Git
No determinable: `D:\Proyectos\Arcanum` tiene `.git` válido pero git lo rechaza por "dubious
ownership" (discrepancia de propietario del directorio en este entorno). Pendiente que el usuario
ejecute `git config --global --add safe.directory "D:/Proyectos/Arcanum"` para poder leer
status/diff/log en futuros checkpoints.

---
Tags: #checkpoint #arcanum
