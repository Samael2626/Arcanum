[build]
builder = "dockerfile"
dockerfilePath = "arcanum-api/Dockerfile"

[deploy]
startCommand = "cd arcanum-api && python -m alembic upgrade head && uvicorn app.main:app --host 0.0.0.0 --port $PORT"
