__pycache__/
*.pyc
.venv/
venv/
.env
.env.*
!.env.example
*.egg-info/
.pytest_cache/
*.log

build/
arcanum_app/.firebase/
arcanum_app/android/.gradle/
# El vault Obsidian (D:\Brain) NO va en el repo; si se copia aquí, se ignora.
Brain/
.dart_tool/
.packages
.flutter-plugins
.flutter-plugins-dependencies
*.iml

# Deploy config (safe to commit without secrets)
!app.py
!Dockerfile
!Procfile
!requirements.txt
!railway.json
!railway.toml
.tmp/

# Lecturas: los JSON de obras se generan con scripts/ingest_library.py
# (1.7 MB cada uno). Se versiona el pipeline, no su salida.
# Salida generada por ingest/translate (1,7 MB por obra), NO se versiona.
# Se ignora el CONTENIDO (/*), no el directorio, para poder exceptuar ficheros.
arcanum-api/scripts/library_data/*
# Excepción: el puente Materia<->Culpeper es contenido curado a mano.
!arcanum-api/scripts/library_data/culpeper_materia_bridge.json

# Fuentes de origen del generador de glifos: 2,2 MB para producir 11 KB.
# Las baja tool/generar_fuente_glifos.py cuando hacen falta.
arcanum_app/.fuentes-origen/

# Las capturas de la pantalla son regenerables con
# flutter test test/capturas --update-goldens --run-skipped
arcanum_app/test/capturas/salida/
