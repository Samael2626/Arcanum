# Passthrough — la lista real vive en la raiz del repo (FUENTE UNICA de verdad).
# Deploys (Render/Railway) y dev local instalan desde /requirements.txt.
# NO agregues paquetes aqui: edita ../requirements.txt.
-r ../requirements.txt
