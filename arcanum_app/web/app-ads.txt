# app-ads.txt de ARCANUM
#
# Desde enero de 2025, AdMob EXIGE este fichero para verificar una app nueva.
# Sin el, la unidad de anuncios no sirve nada aunque este creada y aprobada.
#
# Tiene que quedar servido como texto plano en la RAIZ del dominio que figure
# como sitio web del desarrollador en la ficha de Play:
#
#     https://arcanum-app-magick.web.app/app-ads.txt
#
# CUIDADO con el rewrite de firebase.json: manda todo a /index.html, asi que
# antes de existir este fichero la ruta devolvia HTTP 200 con el HTML de la PWA.
# Un 200 que parece correcto y no lo es. Firebase sirve el estatico antes que el
# rewrite, asi que con el fichero presente se sirve este.
#
# La linea de abajo lleva el ID de editor sacado del App ID de AdMob
# (ca-app-pub-5111199335180018~3542193092 -> pub-5111199335180018).
#
# CONTRASTAR con el fragmento personalizado de la consola de AdMob
# (Aplicaciones > app-ads.txt), que es la fuente autoritativa: si ahi aparece un
# cuarto campo con el ID de autoridad de certificacion, hay que anadirlo aqui.
google.com, pub-5111199335180018, DIRECT
